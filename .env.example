# Application
PORT=3000
NODE_ENV=development

# PostgreSQL (source of truth)
# Host / Postman: localhost. Docker Compose overrides this to @postgres:5432
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/auth_service

# Redis (sessions, refresh tokens, rate limits)
# Host: localhost. Docker Compose overrides to redis://redis:6379
REDIS_URL=redis://localhost:6379

# JWT secrets — use long random strings in real environments
# Generate examples: openssl rand -base64 48
JWT_ACCESS_SECRET=change-me-access-secret-min-32-chars-long
JWT_REFRESH_SECRET=change-me-refresh-secret-min-32-chars-long

# CORS — comma-separated origins, or * for local-only demos
CORS_ORIGIN=http://localhost:3000,http://localhost:5173

# Access token lifetime in seconds (default 15 minutes)
JWT_ACCESS_EXPIRES_IN=900

# Refresh token lifetime in seconds (default 7 days)
JWT_REFRESH_EXPIRES_IN=604800

# SMTP / email (Mailtrap or Ethereal for local)
# Leave SMTP_USER/SMTP_PASS empty in development to auto-use Ethereal test inbox
# Docker Compose points SMTP at Mailpit (mail:1025) — UI at http://localhost:8025
SMTP_HOST=sandbox.smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Auth Service <noreply@example.com>"

# Public base URL (used in verification email text)
APP_URL=http://localhost:3000

# Optional: bcrypt cost factor (10–12 recommended)
BCRYPT_SALT_ROUNDS=12

# PostgreSQL connection pool
DB_POOL_MAX=10
DB_POOL_IDLE_MS=30000
DB_POOL_CONN_TIMEOUT_MS=5000

# Rate limits — fixed window via Redis INCR + EXPIRE (Issue #12)
# Login: 5 attempts / 15 min per IP+email
RL_LOGIN_MAX=5
RL_LOGIN_WINDOW_SEC=900
# Forgot-password: 5 / 15 min per IP
RL_FORGOT_MAX=5
RL_FORGOT_WINDOW_SEC=900
# Register: 10 / 1 hour per IP
RL_REGISTER_MAX=10
RL_REGISTER_WINDOW_SEC=3600
# Resend verification: 5 / 15 min per IP+email
RL_RESEND_MAX=5
RL_RESEND_WINDOW_SEC=900
